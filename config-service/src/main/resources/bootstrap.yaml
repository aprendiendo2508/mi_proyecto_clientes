port:
 server: 8090
spring:
 cloud:
  config:
   server:
    git:
     default-label: config-data
     uri: https://github.com/aprendiendo2508/mi_proyecto_clientes.git
     search-paths: config-data
application:
 name: config-service          
   
